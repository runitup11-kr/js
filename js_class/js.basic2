/**
 * 
 * Naming Conventions 변수 이름 짓는 법
 *  
 * 특수기호는 언더스코어 , 달러 사용 가능
 * 숫자로 이름을 시작할 수 없다
 *      1Name 2Hello
 * 키워드는 변수명으로 사용 할 수 없다.
 *   var let = let;
 */


let keepgoing = '킵고잉';
console.log(keepgoing);

/**
 * Naming Convention 2
 * 
 * 1 ) camelCase - 대부분 언어에서 많이 사용 띄어쓰기 부분 대문자로 표기
 * 2 ) snake_case - 다른 단어 끼리 언더바로 묶어주기 파이썬 언어
 * 3 ) PascalCase - 첫번째 단어도 대문자로 
 * 
 */

const keepGoing = ' ㅋㄱㅇ ';
console.log(keepGoing)
